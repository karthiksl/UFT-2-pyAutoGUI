"""Page Object Model modules — one per Epic Hyperdrive screen.

Each submodule exposes Visual OR logical names as UPPER_CASE constants so
business code can reference ``poms.enrollment.SUBMIT_BTN`` instead of the
raw string ``"enrollment.submit_enrollment_button"``.
"""
